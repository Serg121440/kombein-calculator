# No server-side environment variables required.
# API keys are supplied by the user in the browser and proxied through Next.js API routes.
# Add variables here if you later integrate server-side secrets (e.g. OAuth for ad APIs).
